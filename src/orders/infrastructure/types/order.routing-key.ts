enum OrderRoutingKey {
  ORDERS_REQUEST_ORDER_UPDATE_UUID = "orders.request.order.update.uuid",
  ORDERS_RESPONSE_ORDER_UPDATE_UUID = "orders.response.order.update.uuid",
}

export default OrderRoutingKey;
